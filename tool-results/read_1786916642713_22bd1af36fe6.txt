     1→'use client';
     2→
     3→import { useMemo, useState } from 'react';
     4→import {
     5→  BookOpen,
     6→  CalendarDays,
     7→  Check,
     8→  ChevronDown,
     9→  ChevronLeft,
    10→  ChevronRight,
    11→  Filter,
    12→  Info,
    13→  Layers,
    14→  MapPin,
    15→  Search,
    16→  Sprout,
    17→  X,
    18→} from 'lucide-react';
    19→import { Badge } from '@/components/ui/badge';
    20→import { Button } from '@/components/ui/button';
    21→import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
    22→import { Input } from '@/components/ui/input';
    23→import { Label } from '@/components/ui/label';
    24→import { Separator } from '@/components/ui/separator';
    25→import { copyFor, useTranslation, type Language } from '@/lib/language-store';
    26→import {
    27→  ALGERIA_CALENDAR_MONTHS,
    28→  CALENDAR_ACTION_LABELS,
    29→  CALENDAR_CROP_LABELS,
    30→  CALENDAR_SECTION_LABELS,
    31→  getCalendarCropKeys,
    32→  getCalendarCropLabel,
    33→  type AlgeriaCalendarEntry,
    34→  type CalendarActionType,
    35→} from '@/lib/algeria-crop-calendar';
    36→
    37→const MONTH_ACCENTS = [
    38→  '#0f766e', '#0f766e', '#15803d', '#16a34a', '#65a30d', '#ca8a04',
    39→  '#d97706', '#ea580c', '#c2410c', '#9f1239', '#7c3aed', '#4f46e5',
    40→];
    41→
    42→const ACTION_ORDER: CalendarActionType[] = [
    43→  'sowing',
    44→  'harvest',
    45→  'irrigation',
    46→  'fertilization',
    47→  'soil',
    48→  'weedManagement',
    49→  'maintenance',
    50→  'cropProtection',
    51→  'observation',
    52→];
    53→
    54→type ActionFilter = 'all' | CalendarActionType;
    55→
    56→function localizedCopy(language: Language, copy: { en: string; fr: string; ar: string }): string {
    57→  return copy[language];
    58→}
    59→
    60→function actionIcon(action: CalendarActionType) {
    61→  if (action === 'sowing') return '↗';
    62→  if (action === 'harvest') return '✦';
    63→  if (action === 'irrigation') return '≈';
    64→  if (action === 'fertilization') return '＋';
    65→  if (action === 'soil') return '⌁';
    66→  if (action === 'weedManagement') return '✂';
    67→  if (action === 'maintenance') return '⚙';
    68→  if (action === 'cropProtection') return '⊙';
    69→  if (action === 'observation') return '👁';
    70→  return '•';
    71→}
    72→
    73→function entryMatches(entry: AlgeriaCalendarEntry, selectedCrops: string[], actionFilter: ActionFilter): boolean {
    74→  const cropMatches = selectedCrops.length === 0 || entry.cropKeys.some(key => selectedCrops.includes(key));
    75→  const actionMatches = actionFilter === 'all' || entry.actionTypes.includes(actionFilter);
    76→  return cropMatches && actionMatches;
    77→}
    78→
    79→function EntryCard({ entry, language }: { entry: AlgeriaCalendarEntry; language: Language }) {
    80→  const sectionCopy = CALENDAR_SECTION_LABELS[entry.section];
    81→  return (
    82→    <article className="rounded-xl border border-border bg-card p-3 shadow-sm transition-colors hover:border-emerald-300 dark:hover:border-emerald-800">
    83→      <div className="flex flex-wrap items-start justify-between gap-2">
    84→        <div className="min-w-0">
    85→          <div className="flex flex-wrap items-center gap-1.5">
    86→            <h4 className="text-sm font-semibold text-foreground">{entry.cropContext}</h4>
    87→            <Badge variant="outline" className="text-[10px]">{localizedCopy(language, sectionCopy)}</Badge>
    88→          </div>
    89→          <div className="mt-1 flex flex-wrap gap-1">
    90→            {entry.actionTypes.map(action => (
    91→              <Badge key={action} variant="secondary" className="gap-1 text-[10px] font-normal">
    92→                <span aria-hidden="true">{actionIcon(action)}</span>
    93→                {localizedCopy(language, CALENDAR_ACTION_LABELS[action])}
    94→              </Badge>
    95→            ))}
    96→          </div>
    97→        </div>
    98→        <div className="shrink-0 text-right text-[10px] text-muted-foreground">
    99→          <div>{entry.source.file}</div>
   100→          <div>{entry.source.printedPages}</div>
   101→        </div>
   102→      </div>
   103→      <ul className="mt-3 space-y-1.5 text-xs leading-relaxed text-muted-foreground">
   104→        {entry.operations.map((operation, index) => <li key={`${entry.id}-${index}`} className="flex gap-2"><span className="mt-0.5 text-emerald-600">•</span><span>{operation}</span></li>)}
   105→      </ul>
   106→      <div className="mt-3 flex flex-wrap items-center gap-2 border-t border-border/70 pt-2 text-[10px] text-muted-foreground">
   107→        <BookOpen className="h-3 w-3 text-emerald-600" />
   108→        <span>{copyFor(language, 'Source trace', 'مرجع المصدر', 'Référence source')}</span>
   109→        <span>·</span>
   110→        <span>{entry.source.printedPages}</span>
   111→      </div>
   112→    </article>
   113→  );
   114→}
   115→
   116→export function AlgeriaCropCalendar() {
   117→  const { language, isRTL } = useTranslation();
   118→  const [selectedMonth, setSelectedMonth] = useState(() => new Date().getMonth() + 1);
   119→  const [selectedCrops, setSelectedCrops] = useState<string[]>([]);
   120→  const [cropSearch, setCropSearch] = useState('');
   121→  const [actionFilter, setActionFilter] = useState<ActionFilter>('all');
   122→  const [showCropPicker, setShowCropPicker] = useState(false);
   123→  const [showAnnualView, setShowAnnualView] = useState(false);
   124→
   125→  const allCropKeys = useMemo(() => getCalendarCropKeys(), []);
   126→  const filteredCropKeys = useMemo(() => {
   127→    const query = cropSearch.trim().toLowerCase();
   128→    if (!query) return allCropKeys;
   129→    return allCropKeys.filter(key => {
   130→      const label = getCalendarCropLabel(key, language).toLowerCase();
   131→      const english = CALENDAR_CROP_LABELS[key]?.en.toLowerCase() ?? key;
   132→      return label.includes(query) || english.includes(query) || key.includes(query);
   133→    });
   134→  }, [allCropKeys, cropSearch, language]);
   135→
   136→  const month = ALGERIA_CALENDAR_MONTHS[selectedMonth - 1] ?? ALGERIA_CALENDAR_MONTHS[0];
   137→  const visibleEntries = useMemo(
   138→    () => month.entries.filter(entry => entryMatches(entry, selectedCrops, actionFilter)),
   139→    [month.entries, selectedCrops, actionFilter],
   140→  );
   141→  const allVisibleEntries = useMemo(
   142→    () => ALGERIA_CALENDAR_MONTHS.flatMap(item => item.entries.filter(entry => entryMatches(entry, selectedCrops, actionFilter))),
   143→    [selectedCrops, actionFilter],
   144→  );
   145→  const visibleSections = useMemo(() => Array.from(new Set(visibleEntries.map(entry => entry.section))), [visibleEntries]);
   146→  const filteredMonthCounts = useMemo(() => ALGERIA_CALENDAR_MONTHS.map(item => item.entries.filter(entry => entryMatches(entry, selectedCrops, actionFilter)).length), [selectedCrops, actionFilter]);
   147→  const selectedCropLabels = selectedCrops.map(key => getCalendarCropLabel(key, language));
   148→
   149→  const toggleCrop = (cropKey: string) => {
   150→    setSelectedCrops(current => current.includes(cropKey) ? current.filter(key => key !== cropKey) : [...current, cropKey]);
   151→  };
   152→
   153→  const clearFilters = () => {
   154→    setSelectedCrops([]);
   155→    setCropSearch('');
   156→    setActionFilter('all');
   157→  };
   158→
   159→  const moveMonth = (offset: number) => {
   160→    setSelectedMonth(current => ((current - 1 + offset + ALGERIA_CALENDAR_MONTHS.length) % ALGERIA_CALENDAR_MONTHS.length) + 1);
   161→  };
   162→
   163→  return (
   164→    <div className="space-y-5" dir={isRTL ? 'rtl' : 'ltr'}>
   165→      <Card className="overflow-hidden border-emerald-200/70 shadow-sm dark:border-emerald-900/70">
   166→        <CardHeader className="border-b bg-gradient-to-r from-emerald-50 via-background to-teal-50/60 pb-4 dark:from-emerald-950/30 dark:via-background dark:to-teal-950/20">
   167→          <div className="flex flex-col gap-3 lg:flex-row lg:items-start lg:justify-between">
   168→            <div>
   169→              <CardTitle className="flex items-center gap-2 text-base">
   170→                <CalendarDays className="h-4 w-4 text-emerald-600" />
   171→                {copyFor(language, 'Algeria Agriculture Calendar', 'التقويم الزراعي الجزائري', 'Calendrier agricole algérien')}
   172→              </CardTitle>
   173→              <p className="mt-1 max-w-3xl text-xs leading-relaxed text-muted-foreground">
   174→                {copyFor(language, 'Explore 12 source-backed months, select one crop or several, and compare activities that overlap in the same month. This is an operations calendar, not a companion-planting recommendation.', 'استكشف 12 شهراً موثقاً بالمصادر، واختر محصولاً واحداً أو عدة محاصيل، وقارن الأنشطة المتزامنة في الشهر نفسه. هذا تقويم عمليات وليس توصية بالزراعة المرافقة.', 'Explorez 12 mois documentés, sélectionnez une ou plusieurs cultures et comparez les activités qui se chevauchent le même mois. Il s’agit d’un calendrier d’opérations, pas d’une recommandation d’associations culturales.')}
   175→              </p>
   176→            </div>
   177→            <div className="flex flex-wrap items-center gap-2 text-[10px]">
   178→              <Badge variant="outline" className="gap-1 border-emerald-300 bg-background/70 text-emerald-800 dark:border-emerald-800 dark:text-emerald-200"><BookOpen className="h-3 w-3" />{copyFor(language, '12 PDF sources', '12 مصدر PDF', '12 sources PDF')}</Badge>
   179→              <Badge variant="secondary" className="gap-1"><MapPin className="h-3 w-3" />{copyFor(language, 'Algeria', 'الجزائر', 'Algérie')}</Badge>
   180→            </div>
   181→          </div>
   182→        </CardHeader>
   183→        <CardContent className="space-y-4 p-4 sm:p-5">
   184→          <div className="grid gap-3 lg:grid-cols-[1.2fr_0.8fr]">
   185→            <div>
   186→              <Label className="text-xs font-semibold">{copyFor(language, 'Crop selection', 'اختيار المحاصيل', 'Sélection des cultures')}</Label>
   187→              <div className="mt-1 flex flex-wrap items-center gap-2">
   188→                <Button type="button" variant="outline" size="sm" className="h-9 gap-2" onClick={() => setShowCropPicker(current => !current)} aria-expanded={showCropPicker}>
   189→                  <Sprout className="h-3.5 w-3.5 text-emerald-600" />
   190→                  {selectedCrops.length === 0 ? copyFor(language, 'All crops', 'كل المحاصيل', 'Toutes les cultures') : `${selectedCrops.length} ${copyFor(language, 'selected', 'مختارة', 'sélectionnées')}`}
   191→                  <ChevronDown className="h-3.5 w-3.5" />
   192→                </Button>
   193→                {selectedCrops.length > 0 && <Button type="button" variant="ghost" size="sm" className="h-9 text-xs" onClick={() => setSelectedCrops([])}>{copyFor(language, 'Clear crops', 'مسح المحاصيل', 'Effacer les cultures')}</Button>}
   194→                {selectedCropLabels.slice(0, 5).map((label, index) => <Badge key={selectedCrops[index]} variant="secondary" className="gap-1 text-[10px]">{label}<button type="button" aria-label={`${copyFor(language, 'Remove', 'إزالة', 'Retirer')} ${label}`} onClick={() => toggleCrop(selectedCrops[index])}><X className="h-3 w-3" /></button></Badge>)}
   195→                {selectedCropLabels.length > 5 && <Badge variant="outline" className="text-[10px]">+{selectedCropLabels.length - 5}</Badge>}
   196→              </div>
   197→              {showCropPicker && (
   198→                <div className="mt-2 rounded-xl border border-border bg-background p-3 shadow-sm">
   199→                  <div className="relative">
   200→                    <Search className="pointer-events-none absolute start-3 top-1/2 h-3.5 w-3.5 -translate-y-1/2 text-muted-foreground" />
   201→                    <Input value={cropSearch} onChange={event => setCropSearch(event.target.value)} placeholder={copyFor(language, 'Search crops', 'ابحث عن محصول', 'Rechercher une culture')} className="h-9 ps-9 text-xs" />
   202→                  </div>
   203→                  <div className="mt-3 grid max-h-52 gap-1 overflow-y-auto sm:grid-cols-2 xl:grid-cols-3">
   204→                    {filteredCropKeys.map(cropKey => {
   205→                      const checked = selectedCrops.includes(cropKey);
   206→                      return <button key={cropKey} type="button" onClick={() => toggleCrop(cropKey)} className="flex items-center gap-2 rounded-md px-2 py-2 text-left text-xs hover:bg-muted" aria-pressed={checked}>
   207→                        <span className={`flex h-4 w-4 items-center justify-center rounded border ${checked ? 'border-emerald-600 bg-emerald-600 text-white' : 'border-input'}`}>{checked && <Check className="h-3 w-3" />}</span>
   208→                        <span>{getCalendarCropLabel(cropKey, language)}</span>
   209→                      </button>;
   210→                    })}
   211→                  </div>
   212→                  <div className="mt-2 flex items-center justify-between border-t border-border pt-2 text-[10px] text-muted-foreground">
   213→                    <span>{copyFor(language, 'Select multiple crops to compare same-month activity overlap.', 'اختر عدة محاصيل لمقارنة الأنشطة المتزامنة في الشهر نفسه.', 'Sélectionnez plusieurs cultures pour comparer les activités du même mois.')}</span>
   214→                    <Button type="button" variant="ghost" size="sm" className="h-7 text-[10px]" onClick={() => setShowCropPicker(false)}>{copyFor(language, 'Done', 'تم', 'Terminé')}</Button>
   215→                  </div>
   216→                </div>
   217→              )}
   218→            </div>
   219→            <div>
   220→              <Label className="flex items-center gap-1 text-xs font-semibold"><Filter className="h-3.5 w-3.5" />{copyFor(language, 'Activity filter', 'تصفية النشاط', 'Filtre d’activité')}</Label>
   221→              <select aria-label={copyFor(language, 'Activity filter', 'تصفية النشاط', 'Filtre d’activité')} value={actionFilter} onChange={event => setActionFilter(event.target.value as ActionFilter)} className="mt-1 h-9 w-full rounded-md border border-input bg-background px-3 text-xs">
   222→                <option value="all">{copyFor(language, 'All activities', 'كل الأنشطة', 'Toutes les activités')}</option>
   223→                {ACTION_ORDER.map(action => <option key={action} value={action}>{localizedCopy(language, CALENDAR_ACTION_LABELS[action])}</option>)}
   224→              </select>
   225→            </div>
   226→          </div>
   227→
   228→          <div className="flex flex-wrap items-center justify-between gap-2 rounded-lg border border-dashed border-emerald-300 bg-emerald-50/60 px-3 py-2 text-[11px] dark:border-emerald-800 dark:bg-emerald-950/15">
   229→            <div className="flex items-center gap-2 text-emerald-900 dark:text-emerald-100"><Info className="h-3.5 w-3.5" /><span>{copyFor(language, 'Source language: French PDFs · normalized entries shown with their source wording and page trace.', 'لغة المصدر: ملفات PDF بالفرنسية · تظهر الإدخالات المنظّمة مع صياغة المصدر ورقم الصفحة.', 'Langue source : PDF français · entrées normalisées avec formulation et page de source.')}</span></div>
   230→            {(selectedCrops.length > 0 || actionFilter !== 'all') && <Button type="button" variant="ghost" size="sm" className="h-7 text-[10px]" onClick={clearFilters}>{copyFor(language, 'Reset filters', 'إعادة ضبط المرشحات', 'Réinitialiser les filtres')}</Button>}
   231→          </div>
   232→        </CardContent>
   233→      </Card>
   234→
   235→      <section className="space-y-3">
   236→        <div className="flex flex-wrap items-center justify-between gap-2">
   237→          <div>
   238→            <h3 className="text-sm font-semibold">{copyFor(language, 'Choose a month', 'اختر شهراً', 'Choisir un mois')}</h3>
   239→            <p className="text-[11px] text-muted-foreground">{copyFor(language, 'Month counts reflect the active crop and activity filters.', 'تعكس أعداد الأشهر مرشحات المحاصيل والأنشطة الحالية.', 'Les nombres par mois reflètent les filtres actifs.')}</p>
   240→          </div>
   241→          <Button type="button" variant={showAnnualView ? 'default' : 'outline'} size="sm" className="h-8 gap-1.5 text-xs" onClick={() => setShowAnnualView(current => !current)}><Layers className="h-3.5 w-3.5" />{showAnnualView ? copyFor(language, 'Hide annual overlap', 'إخفاء التداخل السنوي', 'Masquer le chevauchement annuel') : copyFor(language, 'Show annual overlap', 'إظهار التداخل السنوي', 'Afficher le chevauchement annuel')}</Button>
   242→        </div>
   243→        <div className="flex items-center justify-between gap-2 rounded-lg border border-border bg-muted/20 p-2 sm:hidden">
   244→          <Button type="button" variant="outline" size="icon" className="h-9 w-9 shrink-0" onClick={() => moveMonth(-1)} aria-label={copyFor(language, 'Previous month', 'الشهر السابق', 'Mois précédent')}>
   245→            {isRTL ? <ChevronRight className="h-4 w-4" /> : <ChevronLeft className="h-4 w-4" />}
   246→          </Button>
   247→          <div className="min-w-0 text-center" aria-live="polite">
   248→            <div className="truncate text-xs font-semibold">{month.name[language]}</div>
   249→            <div className="text-[10px] text-muted-foreground">{visibleEntries.length} {copyFor(language, 'entries', 'إدخالات', 'entrées')}</div>
   250→          </div>
   251→          <Button type="button" variant="outline" size="icon" className="h-9 w-9 shrink-0" onClick={() => moveMonth(1)} aria-label={copyFor(language, 'Next month', 'الشهر التالي', 'Mois suivant')}>
   252→            {isRTL ? <ChevronLeft className="h-4 w-4" /> : <ChevronRight className="h-4 w-4" />}
   253→          </Button>
   254→        </div>
   255→        <div className="grid grid-cols-4 gap-1.5 sm:grid-cols-4 sm:gap-2 md:grid-cols-6 lg:grid-cols-12">
   256→          {ALGERIA_CALENDAR_MONTHS.map((item, index) => {
   257→            const active = item.number === selectedMonth;
   258→            return <button key={item.number} type="button" onClick={() => setSelectedMonth(item.number)} className={`rounded-lg border p-1.5 text-center transition-all sm:rounded-xl sm:p-2 ${active ? 'border-emerald-500 bg-emerald-50 shadow-sm dark:bg-emerald-950/30' : 'border-border bg-card hover:border-emerald-300'}`} aria-label={`${item.name[language]} — ${filteredMonthCounts[index]} ${copyFor(language, 'entries', 'إدخالات', 'entrées')}`} aria-pressed={active}>
   259→              <span className="mx-auto flex h-6 w-6 items-center justify-center rounded-full text-[10px] font-bold text-white sm:h-7 sm:w-7 sm:text-xs" style={{ backgroundColor: MONTH_ACCENTS[index] }}>{item.number}</span>
   260→              <span className="mt-1 block truncate text-[9px] font-semibold sm:text-[10px]">{item.name[language]}</span>
   261→              <span className="mt-0.5 block text-[9px] text-muted-foreground sm:text-[10px]">{filteredMonthCounts[index]}</span>
   262→            </button>;
   263→          })}
   264→        </div>
   265→      </section>
   266→
   267→      {showAnnualView && (
   268→        <Card className="overflow-hidden border-indigo-200/70 dark:border-indigo-900/70">
   269→          <CardHeader className="border-b bg-indigo-50/60 pb-3 dark:bg-indigo-950/20"><CardTitle className="flex items-center gap-2 text-sm"><Layers className="h-4 w-4 text-indigo-600" />{copyFor(language, 'Annual same-month activity overlap', 'تداخل الأنشطة المتزامنة على مدار السنة', 'Chevauchement annuel des activités du même mois')}</CardTitle><p className="text-[11px] leading-relaxed text-muted-foreground">{copyFor(language, 'A filled cell means the selected crop has at least one source entry in that month. It does not mean the crops are biologically compatible as companions.', 'تعني الخلية المملوءة وجود إدخال مصدر واحد على الأقل للمحصول المختار في ذلك الشهر. ولا تعني أن المحاصيل متوافقة بيولوجياً كزراعة مرافقة.', 'Une cellule remplie signifie qu’une culture sélectionnée possède au moins une entrée source ce mois-là. Cela ne signifie pas que les cultures sont biologiquement compatibles en association.')}</p></CardHeader>
   270→          <CardContent className="overflow-x-auto p-2 sm:p-4">
   271→            {selectedCrops.length === 0 ? <p className="rounded-lg border border-dashed p-4 text-center text-xs text-muted-foreground">{copyFor(language, 'Select one or more crops above to see the annual overlap matrix.', 'اختر محصولاً واحداً أو أكثر لرؤية مصفوفة التداخل السنوية.', 'Sélectionnez une ou plusieurs cultures pour voir la matrice annuelle.')}</p> : <table aria-label={copyFor(language, 'Annual crop overlap matrix', 'مصفوفة التداخل السنوي للمحاصيل', 'Matrice annuelle de chevauchement des cultures')} className="min-w-[760px] w-full border-collapse text-[10px]"><thead><tr><th className="sticky start-0 bg-background p-2 text-start font-semibold">
   272→{copyFor(language, 'Crop', 'المحصول', 'Culture')}</th>{ALGERIA_CALENDAR_MONTHS.map(item => <th key={item.number} className="p-2 text-center font-semibold">{item.name[language].slice(0, 3)}</th>)}</tr></thead><tbody>{selectedCrops.map(cropKey => <tr key={cropKey} className="border-t border-border"><th className="sticky start-0 bg-background p-2 text-start font-medium">
   273→{getCalendarCropLabel(cropKey, language)}</th>{ALGERIA_CALENDAR_MONTHS.map(item => { const count = item.entries.filter(entry => entry.cropKeys.includes(cropKey) && entryMatches(entry, selectedCrops, actionFilter)).length; return <td key={item.number} className="p-1 text-center"><button type="button" disabled={count === 0} onClick={() => { setSelectedMonth(item.number); setShowAnnualView(false); }} className={`mx-auto flex h-7 min-w-7 items-center justify-center rounded-md ${count > 0 ? 'bg-emerald-100 font-bold text-emerald-800 hover:bg-emerald-200 dark:bg-emerald-900/50 dark:text-emerald-200' : 'text-muted-foreground/40'}`}>{count || '—'}</button></td>; })}</tr>)}</tbody></table>}
   274→          </CardContent>
   275→        </Card>
   276→      )}
   277→
   278→      <section className="space-y-3">
   279→        <div className="flex flex-col gap-2 sm:flex-row sm:items-end sm:justify-between">
   280→          <div>
   281→            <h3 className="text-sm font-semibold">{month.name[language]} <span className="font-normal text-muted-foreground">· {copyFor(language, `${visibleEntries.length} source entries`, `${visibleEntries.length} إدخالاً من المصدر`, `${visibleEntries.length} entrées source`)}</span></h3>
   282→            <p className="text-[11px] text-muted-foreground">{month.source.file} · {month.source.printedPages} · {month.source.pdfLength}</p>
   283→          </div>
   284→          <Badge variant="outline" className="gap-1 text-[10px]"><BookOpen className="h-3 w-3" />{copyFor(language, 'Source-traceable', 'موثق بالمصدر', 'Traçable par source')}</Badge>
   285→        </div>
   286→        {visibleEntries.length === 0 ? <Card><CardContent className="p-8 text-center text-xs text-muted-foreground">{copyFor(language, 'No source entries match the current filters. Try another crop or activity.', 'لا توجد إدخالات مصدر تطابق المرشحات الحالية. جرّب محصولاً أو نشاطاً آخر.', 'Aucune entrée source ne correspond aux filtres actuels. Essayez une autre culture ou activité.')}</CardContent></Card> : <div className="space-y-4">{visibleSections.map(section => { const entries = visibleEntries.filter(entry => entry.section === section); return <div key={section} className="space-y-2"><div className="flex items-center gap-2"><Separator className="flex-1" /><span className="text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">{localizedCopy(language, CALENDAR_SECTION_LABELS[section])}</span><Separator className="flex-1" /></div><div className="grid gap-3 lg:grid-cols-2">{entries.map(entry => <EntryCard key={entry.id} entry={entry} language={language} />)}</div></div>; })}</div>}
   287→      </section>
   288→
   289→      <Card className="border-amber-200/80 dark:border-amber-900/70">
   290→        <CardHeader className="pb-3"><CardTitle className="flex items-center gap-2 text-sm"><Info className="h-4 w-4 text-amber-600" />{copyFor(language, 'Month source notes and boundaries', 'ملاحظات المصدر وحدوده لهذا الشهر', 'Notes et limites de la source du mois')}</CardTitle></CardHeader>
   291→        <CardContent className="grid gap-4 p-4 pt-0 lg:grid-cols-3">
   292→          <div><h4 className="text-[11px] font-semibold">{copyFor(language, 'Source note', 'ملاحظة المصدر', 'Note de source')}</h4><ul className="mt-1 space-y-1 text-[11px] leading-relaxed text-muted-foreground">{(month.sourceNote.length ? month.sourceNote : [copyFor(language, 'No separate month-specific note was printed in the normalized record.', 'لا توجد ملاحظة شهرية منفصلة في السجل المنظم.', 'Aucune note mensuelle distincte dans le relevé normalisé.')]).map((note, index) => <li key={index}>• {note}</li>)}</ul></div>
   293→          <div><h4 className="text-[11px] font-semibold">{copyFor(language, 'Regional / production context', 'السياق الإقليمي والإنتاجي', 'Contexte régional / de production')}</h4><ul className="mt-1 space-y-1 text-[11px] leading-relaxed text-muted-foreground">{(month.regionalQualifiers.length ? month.regionalQualifiers : [copyFor(language, 'Regional qualifiers are attached to individual source entries where printed.', 'تظهر المؤهلات الإقليمية مع إدخالات المصدر حيث وردت مطبوعة.', 'Les qualificatifs régionaux sont attachés aux entrées source lorsqu’ils sont imprimés.')]).map((note, index) => <li key={index}>• {note}</li>)}</ul></div>
   294→          <div><h4 className="text-[11px] font-semibold">{copyFor(language, 'Uncertainty and companion-planting boundary', 'حدود عدم اليقين والزراعة المرافقة', 'Limites d’incertitude et d’association')}</h4><ul className="mt-1 space-y-1 text-[11px] leading-relaxed text-muted-foreground">{(month.uncertaintyNotes.length ? month.uncertaintyNotes : [copyFor(language, 'The source is a monthly operations guide; it does not establish companion-planting compatibility.', 'المصدر دليل عمليات شهرية؛ ولا يثبت توافق الزراعة المرافقة.', 'La source est un guide mensuel d’opérations ; elle ne prouve pas la compatibilité des associations.')]).map((note, index) => <li key={index}>• {note}</li>)}{month.companionNote.slice(0, 1).map((note, index) => <li key={`companion-${index}`}>• {note}</li>)}</ul></div>
   295→        </CardContent>
   296→      </Card>
   297→
   298→      <Card className="border-sky-200/80 dark:border-sky-900/70">
   299→        <CardHeader className="pb-3"><CardTitle className="flex items-center gap-2 text-sm"><BookOpen className="h-4 w-4 text-sky-600" />{copyFor(language, 'Source provenance', 'مصدر البيانات', 'Provenance de la source')}</CardTitle><p className="text-[11px] leading-relaxed text-muted-foreground">{copyFor(language, 'Original institution, document title, source language, and extraction method for this month’s data.', 'الجهة الأصلية وعنوان الوثيقة ولغة المصدر وطريقة استخراج بيانات هذا الشهر.', 'Institution d’origine, titre du document, langue source et méthode d’extraction pour les données de ce mois.')}</p></CardHeader>
   300→        <CardContent className="grid gap-4 p-4 pt-0 lg:grid-cols-2">
   301→          <div><h4 className="text-[11px] font-semibold">{copyFor(language, 'Institution', 'الجهة', 'Institution')}</h4><p className="mt-1 text-[11px] leading-relaxed text-muted-foreground">{month.source.institution}</p></div>
   302→          <div><h4 className="text-[11px] font-semibold">{copyFor(language, 'Document title', 'عنوان الوثيقة', 'Titre du document')}</h4><p className="mt-1 text-[11px] leading-relaxed text-muted-foreground">{month.source.documentTitle}</p><div className="mt-1 flex flex-wrap items-center gap-1.5 text-[10px]"><Badge variant="outline" className="gap-1 border-sky-300 bg-sky-50/70 text-sky-800 dark:border-sky-800 dark:bg-sky-950/30 dark:text-sky-300"><MapPin className="h-2.5 w-2.5" />{month.source.language}</Badge><Badge variant="outline" className="gap-1 text-[10px]">{month.source.pdfLength}</Badge><Badge variant="outline" className="gap-1 text-[10px]">{month.source.printedPages}</Badge></div></div>
   303→          <div><h4 className="text-[11px] font-semibold">{copyFor(language, 'Extraction status', 'حالة الاستخراج', 'Statut d’extraction')}</h4><p className="mt-1 text-[11px] leading-relaxed text-muted-foreground">{month.source.extractionStatus}</p></div>
   304→          <div><h4 className="text-[11px] font-semibold">{copyFor(language, 'Interpretation rule', 'قاعدة التفسير', 'Règle d’interprétation')}</h4><p className="mt-1 text-[11px] leading-relaxed text-muted-foreground">{month.source.interpretationRule}</p></div>
   305→        </CardContent>
   306→      </Card>
   307→
   308→      <div className="flex items-start gap-2 rounded-xl border border-border bg-muted/20 p-3 text-[10px] leading-relaxed text-muted-foreground"><Info className="mt-0.5 h-3.5 w-3.5 shrink-0 text-emerald-600" /><p>{copyFor(language, 'Safety boundary: phytosanitary entries point to the source annex or leaflet when no product, active ingredient, or dose is printed. Use the INPV-aware safety tools for product decisions; this calendar does not invent treatments.', 'حد السلامة: تشير إدخالات وقاية النبات إلى الملحق أو النشرة المصدرية عندما لا يطبع المصدر منتجاً أو مادة فعالة أو جرعة. استخدم أدوات السلامة المرتبطة بفهرس INPV لقرارات المنتجات؛ هذا التقويم لا يخترع علاجات.', 'Limite de sécurité : les entrées phytosanitaires renvoient à l’annexe ou à la fiche source lorsque produit, matière active ou dose ne sont pas imprimés. Utilisez les outils de sécurité liés à l’INPV ; ce calendrier n’invente pas de traitements.')}</p></div>
   309→
   310→      <div className="sr-only" aria-live="polite">{allVisibleEntries.length} {copyFor(language, 'matching source entries across the year', 'إدخالات مصدر مطابقة على مدار السنة', 'entrées source correspondantes sur l’année')}</div>
   311→    </div>
   312→  );
   313→}
   314→